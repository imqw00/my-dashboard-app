{
  "name": "my-dashboard-app",
  "version": "1.0.0",
  "description": "My Dashboard App with proxy and UI",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js && cp index.html dist/index.html",
    "start": "serve dist"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    // your runtime dependencies here, e.g. express, axios, etc.
  },
  "devDependencies": {
    "esbuild": "^0.17.19",
    "serve": "^14.2.0"
  }
}
